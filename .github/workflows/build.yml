name: Build Desktop

on:
  # GitHub Actions 页面手动运行
  workflow_dispatch:

  # 打版本标签时自动构建
  # 例如：v0.1.0、v0.2.0
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build ${{ matrix.name }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            name: Windows

          - os: macos-latest
            name: macOS

          - os: ubuntu-latest
            name: Linux

    runs-on: ${{ matrix.os }}

    steps:
      # 1. 拉取仓库代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 安装 Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # 3. 安装 npm 依赖
      - name: Install dependencies
        run: npm ci

      # 4. 构建 Web 前端
      - name: Build Web
        run: npm run build

      # 5. Electron 桌面端打包
      # --publish never 防止 electron-builder
      # 在 CI 环境下自动尝试发布 GitHub Release
      - name: Build Desktop
        run: npm run desktop:dist -- --publish never

      # 6. 上传打包结果到 GitHub Actions Artifacts
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: MeetingNotes-${{ matrix.name }}
          path: release/desktop/**
          if-no-files-found: error
